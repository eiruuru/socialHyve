# iOS native project

Run on macOS with CocoaPods installed:

```bash
brew install cocoapods
npx cap add ios
npm run cap:sync
```

The Android project is scaffolded under `../android/`. iOS is generated locally when CocoaPods is available.
